# Listas .NET

### ArrayList

```
var list = new ArrayList(); //Array que aceita qualquer tipo.

list.Add("Marcus"); //Adiciona elemento

list.Remove(40);//Remove um elemento a partir de seu valor. 

list.RemoveAt(0);//Remove a partir do índice.

list.Clear() //Limpa toda a lista. 

foreach (var item in list) //Imprime todos os valores.
{
    System.Console.WriteLine(item);
}
```
---
